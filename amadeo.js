function compare(str1, str2) {
    if (str1 === str2) {
        return true;
    } else {
        return false;
    };
}

const str1 = "madUnyu";
const str2 = "ganteng";

console.log(compare(str1, str2));
